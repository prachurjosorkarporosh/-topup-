{
  "name": "dream-topup",
  "version": "1.0.0",
  "description": "Dream TopUp web application",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "esbuild src/firebase.js --bundle --minify --outfile=firebase-bundle.js --format=iife",
    "lint": "node --check server.js && node --check js/app.js && node --check js/admin.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase": "^12.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
